# 二十五、Day12 作业
#
# 今天作业建议直接放进你之前的 order_tool 项目，或者单独建立 day12_concurrency.py。
#
# 作业 1：线程池基础
#
# 实现：
#
# def fetch_order_count(site_id: int) -> int:
#     ...
#
# 要求：
#
# 模拟 5 个站点；
#
# 使用 ThreadPoolExecutor(max_workers=3)；
#
# 每个站点返回订单数量；
#
# 使用 as_completed()；
#
# 最后统计总订单数。
#
# 作业 2：异常隔离
#
# 在作业1基础上，让站点3抛出异常。
#
# 要求：
#
# 其他站点继续执行；
#
# 记录失败站点；
#
# 最终输出成功数、失败数和订单总数；
#
# 不允许一个站点失败导致整个批次直接退出。
#
# 作业 3：共享状态与锁
#
# 实现 5 个线程共同修改计数器。
#
# 要求：
#
# 每个线程执行 10,000 次；
#
# 使用 Lock；
#
# 最终结果正确；
#
# 解释为什么不能依赖 GIL 保证业务正确性。
#
# 作业 4：进程池
#
# 实现：
#
# def calculate_sum(limit: int) -> int:
#     ...
# 
# 要求：
#
# 使用 ProcessPoolExecutor；
#
# 提交 4 个 CPU 计算任务；
#
# 获取所有结果；
#
# 使用 if __name__ == "__main__"；
#
# 解释为什么进程池任务函数不能随便写成局部函数或 lambda。
#
# 作业 5：思考题
#
# 假设你要同步 200 个 Shopify 站点，每个站点平均请求 1 秒，每个站点还有独立 API 限流。
#
# 请回答：
#
# 应该使用线程池还是进程池？为什么？max_workers 应该如何确定？如果某个站点失败，如何保证其他站点继续？如何避免重复同步订单？
#
# 这道题是今天最接近真实后端开发的一道题。