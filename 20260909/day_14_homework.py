# 十四、Day14 作业
# 作业 1：基础改造（重点）
#
# 在当前项目中增加订单数量 quantity，要求必须为正整数，并增加 total_amount 属性，计算 amount * quantity。
#
# 作业 2：可靠性改造（重点）
#
# 给接口拉取增加最多 3 次重试，要求超时可以重试，订单校验失败不重试，并记录实际重试次数。
#
# 作业 3：并发控制（重点）
#
# 使用 Semaphore(3) 同步 10 个店铺，编写测试证明同时进入接口的请求数量不超过 3。
#
# 作业 4：幂等测试（重点）
#
# 同一个 OrderSyncService 连续执行两次同步，要求第二次不再新增订单，并解释 valid 与 saved 为什么可能不同。
#
# 作业 5：批处理扩展（次重点）
#
# 把批次大小设置为 2，记录每次保存的批次大小，验证 5 条订单会拆成 2、2、1 三批。
#
# 作业 6：工程思考（次重点）
#
# 回答：如果同步 1000 个店铺，每个店铺有 10 万条订单，当前 gather() 版本会出现什么问题？你会如何改造为“分页拉取 + 有界队列 + 批量保存”？
#
# 十五、Day14 最终验收标准
#
# 你不需要把所有语法背下来，但应该能够不看答案完成以下能力：
#
# 重点： 能独立解释整个订单同步流程，以及每个模块的职责。
#
# 重点： 能正确使用 async/await、Semaphore、wait_for() 组织并发拉取。
#
# 重点： 能实现数据清洗、业务校验、去重和批处理。
#
# 重点： 能区分去重与幂等，并说明数据库唯一约束的作用。
#
# 重点： 能处理单店铺失败、单订单失败，避免一个错误导致全部任务中断。
#
# 重点： 能编写正常、异常、重复执行三类测试。
#
# 次重点： 能解释线程池、异步和进程池的选择依据。
#
# 次重点： 能通过 CLI 运行程序并读取统计结果。
#
# 了解即可： 能说出生产化还需要分页、事务、限流、断点续传和监控。
#
# 完整性自检
#
# 本次已覆盖原定的 “订单同步与批处理工具”综合实战，并把 Day1–11 的数据模型、异常、生成器、模块化、测试，与 Day12–13 的并发和异步连接起来。知识地图中的 12 个模块均已展开；核心代码包含模型、清洗、接口、仓储、服务、入口和测试；可靠性部分明确了教学实现与生产实现的差异。
#
# 至此，前 14 天的 Python 基础与高级能力学习可以形成一个阶段闭环。下一阶段进入 Agent 时，你已经具备编写工具函数、调用外部 API、组织异步任务、处理数据和构建可测试工程的基础。